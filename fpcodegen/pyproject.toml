[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "fpcodegen"
version = "0.1.0"
description = "Code generation utilities for floating-point benchmarks"
requires-python = ">=3.7"

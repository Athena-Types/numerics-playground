#include "float_ops/addfp64.fz"

function delta_1
   (a : (![1.0]num, num))
{
  let (x1', x2) = a;
  let [x1] = x1';

  s1 = addfp64(|x2, x1|);
  s1
}

function delta
   (x1' : ![1.0]num) (x2 : num)
{
  d1 = delta_1(x1', x2);
  // d1 = delta_1(x1')(x2);
  d1
}

delta
